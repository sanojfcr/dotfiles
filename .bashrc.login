#### .bashrc.login ####
# **** 問題少なく SHIROKANE を利用するためのアドバイス:
# **** Advice for using SHIROKANE without stress:
#
#   .bashrc.login には ログインノードのシェル変数や
#    エイリアスの設定を記述します。
#   In .bashrc.login, describe the settings of shell
#   variables and the aliases on the login nodes.
#
#   ログインノードでの環境変数の設定についての記述は、
#   .bash_profile.login にしてください。
#   Describe the environment variable settings of
#   login nodes in .bash_profile.login.

# ログインノードのシェル変数やエイリアスの設定
